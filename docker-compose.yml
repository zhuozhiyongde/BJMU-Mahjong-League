services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - bjmu-league-data:/app/data
    environment:
      - NODE_ENV=production
    restart: unless-stopped

volumes:
  bjmu-league-data:
